export class Ingredient {
  // Poniendo public en los argumentos del constructor es una forma abreviada de definirte primero las variables y asignarle los valores de los argumentos del constructor
  constructor(public name: string, public amount: number) {}
}
